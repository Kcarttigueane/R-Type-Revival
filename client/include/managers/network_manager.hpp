#if !defined(NETWORK_MANAGER_HPP)
#    define NETWORK_MANAGER_HPP

class NetworkManager {
private:
    /* data */
public:
    NetworkManager(/* args */) = default;
    ~NetworkManager() = default;
};

#endif  // NETWORK_MANAGER_HPP
