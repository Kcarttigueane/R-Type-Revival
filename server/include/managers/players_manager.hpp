#if !defined(PLAYERS_MANAGER_HPP)
#    define PLAYERS_MANAGER_HPP

class PlayersManager {
private:
    /* data */
public:
    PlayersManager(/* args */) = default;
    ~PlayersManager() = default;
};

#endif  // PLAYERS_MANAGER_HPP
